in this pattern without using strategy design pattern , we are seeing duplicacy of code which is not good 
for big and complex applications, 
so we use this pattern to avoid duplicacy and increase reusability of code using interfaces
