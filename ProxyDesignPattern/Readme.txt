The Proxy Design Pattern is a structural design pattern used in object-oriented programming. It provides a substitute or placeholder for another object to control access to it.

✅ Definition:
A Proxy is a class that functions as an interface to something else. The proxy could be controlling access, managing resource usage, or adding functionality like logging, lazy loading, or security checks.

🔧 Use Case Scenarios:
Virtual Proxy – Lazy loading of heavyweight objects.

Protection Proxy – Access control (authorization).

Remote Proxy – Representing an object in a different address space (e.g., client-server).

Smart Proxy – Adds extra logic (like caching, logging, reference counting) when accessing the real object.

It is used in caching data , providing access restrictions and many more uses..