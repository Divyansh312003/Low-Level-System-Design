 Liskov's substitution principle says that if another object of a class is replaced by an object
 then it must expand the capabilities of the parent class rather than narrowing it down


 In the example we saw that bicycle implements the hasEngine function which it does not need to implement
 but it has to implement it due to interface so this becomes the issue